## Review
- Correct: Source-aware resolution is implemented coherently. `DockDecisionEngine.resolvedScrollDeltaWithSource` tags AppKit deltas as `.appKit` and `shouldApplyDiscreteScrollInversion` skips remapper inversion for that source, while preserving inversion for raw fallback/majority sources (`Dockmint/DockDecisionEngine.swift:230-261`, `Dockmint/DockDecisionEngine.swift:320-340`). This directly addresses the LinearMouse/Mos double-inversion mechanism described in the plan.
- Correct: `DockClickEventTap` now passes the sourced delta through the new effective-delta overload and logs the selected source, requested/applied inversion, source PID/bundle, remapper-running state, AppKit/CG axis fields, effective delta, final direction, and continuous state (`Dockmint/DockClickEventTap.swift:323-343`, `Dockmint/DockClickEventTap.swift:387`). This is useful for Parallels/remapper diagnosis.
- Correct: The GUI automation parser remains compatible: `scripts/automated_scroll_direction_checks.sh` still looks for `DockClickEventTap: Raw scroll at`, `continuous: true|false`, and `dir: up|down` (`scripts/automated_scroll_direction_checks.sh:98-110`), all of which remain present in the updated log line (`Dockmint/DockClickEventTap.swift:387`).
- Correct: Tests cover both AppKit-positive and AppKit-negative remapper cases, raw fallback inversion, continuous non-inversion, and Shift/alternate-axis AppKit source behavior (`DockmintTests/DockDecisionEngineXCTest.swift:423-562`). Existing resolver tests remain in place (`DockmintTests/DockDecisionEngineXCTest.swift:564-620`).
- Note (Medium): The hidden/user default override no longer guarantees inversion when AppKit interpreted deltas are present. `shouldInvertDiscreteScrollDirection` still treats `userOverride` as a true inversion request (`Dockmint/DockDecisionEngine.swift:305-317`), but `shouldApplyDiscreteScrollInversion` then suppresses it for `.appKit` sources (`Dockmint/DockDecisionEngine.swift:320-324`). Since `DockClickEventTap` now uses the sourced overload (`Dockmint/DockClickEventTap.swift:341-343`), users relying on `invertDiscreteScrollDirection` to force-flip ordinary discrete events with nonzero `NSEvent.scrollingDeltaY` will see changed behavior. This may be intentional narrowing under the plan, but it is not explicitly tested or documented.
- Note (Low): Below-threshold scroll events log only raw/source/inversion/effective fields (`Dockmint/DockClickEventTap.swift:376-378`), not the full Parallels diagnostic context included in the main raw-scroll line. If VM/remapper events are unexpectedly filtered by threshold, diagnosis may still require adding axis/source PID/bundle/continuous fields to that log.
- Test: Ran `xcodebuild -project Dockmint.xcodeproj -scheme Dockmint -configuration Debug -only-testing:DockmintTests/DockDecisionEngineXCTest test`; all 31 selected tests passed. Xcode emitted a CoreSimulator out-of-date warning, but the macOS test destination completed successfully.
- Blocker: None found. No blocking issues for the reviewed diff.
